(function(window){
	'use strict';

	if( window.OSP ){
		if( OSP.Editor )	return;
	}
	else
		window.OSP = {};
	
	OSP.Editor = function( namespace, params ){
		var ns = namespace;
		var Editor = this;
		var connector;
		var dataType;
		var currentData;
		var mode;
		var saveAction;
		var fileExplorerDialogSection;
		var fileExplorerId;
		var eventEnable;
		
		
		
	};

})(window);